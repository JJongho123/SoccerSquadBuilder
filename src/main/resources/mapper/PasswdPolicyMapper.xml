<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ssb.soccer.passwdPolicy.mapper.PasswdPolicyMapper">

    <!-- 모든 사용자 조회 -->
    <select id="findAllDatas" resultType="ssb.soccer.passwdPolicy.model.PasswdPolicy">
        SELECT * FROM passwd_policy WHERE is_active = 1 ORDER BY id;
    </select>


</mapper>
